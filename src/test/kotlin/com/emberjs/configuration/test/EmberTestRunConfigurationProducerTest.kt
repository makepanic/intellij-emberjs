package com.emberjs.configuration.test

import org.assertj.core.api.Assertions.assertThat
import org.junit.Test

class EmberTestRunConfigurationProducerTest {
//    @Test fun `extracts moduleFor with options`() = doTest("""
//import { moduleFor, test } from 'ember-qunit';
//
//moduleFor('route:items', 'Unit | Route | items', {
//  // Specify the other units that are required for this test.
//  // needs: ['controller:foo']
//});
//
//""".trimIndent(), "Unit | Route | items")
//
//    @Test fun `extracts moduleFor without options`() = doTest("""
//import { moduleFor, test } from 'ember-qunit';
//
//moduleFor('route:items', 'Unit | Route | items');
//
//""".trimIndent(), "Unit | Route | items")
//
//    @Test fun `extracts moduleForModel with options`() = doTest("""
//import { moduleForModel, test } from 'ember-qunit';
//
//moduleForModel('issue', 'Unit | Model | issue', {
//  // Specify the other units that are required for this test.
//  needs: []
//});
//
//""".trimIndent(), "Unit | Model | issue")
//
//
//    private fun doTest(text: String, expected: String) {
//        assertThat(EmberTestRunConfigurationProducer.moduleFrom(text))
//                .isEqualTo(expected)
//    }

}